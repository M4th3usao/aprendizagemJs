// Sempre é bom lembrar que o nome de variável conta muito!

const nome = 'Luiz';// string
const nome1 = 'Luiz';// string
const nome2 = 'Luiz';// string
const num = 10;//number
const num2 = 10.53; //number
let nomeAluno; // undefined = não aponta pra local nenhum na memória
let sobrenomeAluno = null; // não aponta pra local nenhum na memória, mas é utilizado para desconfigurar uma variável
const aprovado = false; // boolean = valor boleano = verdadeiro ou falso, ou seja, valores lógicos.


// Dados primitivos: String, Number, Null, Boolean e Symbol.