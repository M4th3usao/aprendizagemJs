// Diferenças entre var e let

// Não crie variáveis globais, ou seja, sem const ou let para não gerar futuros erros no código.
// com o var é possível declarar e redeclarar, já com o let não é possível redeclarar! Essa é a principal diferença.